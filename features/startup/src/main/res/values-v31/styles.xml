<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.App.Starting"
        parent="Theme.SplashScreen">

        <!-- splash screen background-->
        <item name="windowSplashScreenBackground">@color/background</item>

        <!-- logo drawable + animated override for v31 -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/passbolt_splash_logo</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>

        <!-- bottom branding image -->
        <item name="android:windowSplashScreenBrandingImage">@drawable/branding_image</item>

        <!-- theme of the activity shown after splash -->
        <item name="postSplashScreenTheme">@style/Theme.Passbolt</item>
    </style>
</resources>
